# Проекты: чеклист после реализации и перед эксплуатацией

Дата: 2026-04-08. Дополняет [`projects-predeploy-checklist.md`](./projects-predeploy-checklist.md) (инфраструктура) и [`projects-foundation.md`](./projects-foundation.md) §8 (регрессия).

---

## 1. Что уже есть в коде (кратко)

| Область | Статус |
|--------|--------|
| Карточка проекта, статус, мяч, архив (`archivedAt`), поля шапки | ✅ |
| Список `/projects`, деталь `/projects/[id]`, меню для WOWSTORG, обновлённый главный экран и управление проектами | ✅ |
| Журнал активности (`ProjectActivityLog`), в т.ч. заявка создана / отменена | ✅ |
| Заявки с `projectId`, каталог `?projectId=`, корзина по scope, вкладки active/archive в очереди заявок | ✅ |
| Контакты ЛПР, записи переговоров | ✅ |
| Файлы и папки (bucket `project-files`, лимиты, API, UI) | ✅ |
| Смета: версии, разделы LOCAL/REQUISITE, строки, PDF (15% комиссия), копия из заявки при `projectId`, primary-версия, удаление версии, импорт из выбранных заявок | ✅ |
| Тайминг: дни/слоты, экспорт .docx, двухколоночный список слотов, автопредзаполнение следующего интервала | ✅ |

---

## 2. Что ещё можно усилить позже (не блокер эксплуатации)

Из [`projects.md`](./projects.md):

1. **Двусторонний синк** строк блока «Реквизит» ↔ `OrderLine` при правках со склада или из сметы (сейчас основной сценарий остаётся управляемым через проектные версии и ручной импорт из выбранных заявок, без полного автосинка всех правок).
2. **Шаблон оформления** PDF/Word, импорт xlsx в строки, напоминания по проекту — по бэклогу продукта.

Файлы в папке **Сметы** по-прежнему можно использовать параллельно с конструктором.

---

## 3. Чеклист: что сделать вам после мержа / перед продом

### База данных

1. На **каждой** среде (staging → production) применить миграции Prisma: `npm run db:deploy` или ваш CI-эквивалент.
2. Убедиться, что нет «висящих» частичных миграций: `npx prisma migrate status`.
3. При необходимости — сид демо-данных (проекты в сид не обязательны).

### Supabase Storage

1. Создать bucket **`project-files`** (или имя из `SUPABASE_STORAGE_PROJECTS_BUCKET`).
2. Политики доступа — в духе приватных бакетов приложения (доступ через сервер с service role), не путать с публичными URL без необходимости.
3. См. подробнее [`projects-predeploy-checklist.md`](./projects-predeploy-checklist.md).

### Переменные окружения (Vercel / сервер)

1. **`SUPABASE_STORAGE_PROJECTS_BUCKET`** — задана для Preview и Production и совпадает с реальным именем бакета в соответствующем проекте Supabase.
2. **`SUPABASE_URL`**, **`SUPABASE_SERVICE_ROLE_KEY`** — как для остальных загрузок.
3. После изменения env — **пересобрать** деплой.

### Синхронизация документации для агентов

1. Выполнить **`npm run brain:inventory`** после добавления маршрутов API или новых `process.env.*`, чтобы инвентарь в `brain/` оставался актуальным.

### Дымовые проверки (ручные, WOWSTORG)

1. Открыть главный экран WOWSTORG → убедиться, что статус инвентарного аудита встроен в шапку блока реквизита, кнопка **«Управление проектами»** ведёт на `/projects`, архивные кнопки убраны, а **«Каталог»** визуально выделен.
2. Открыть **«Управление проектами»** → создать проект → проверить combobox заказчика: выбор из списка и ручной ввод нового имени; убедиться, что появились папки **Сметы / Дизайн / Документация**.
3. Загрузить файл в папку → скачать → удалить; проверить лимиты (сообщение при превышении 15 файлов или 200 МБ) и что кнопка **«Загрузить»** сразу открывает file picker.
4. **Каталог** с `?projectId=` → оформить заявку → в карточке проекта видна заявка, в журнале — «Создана заявка».
5. Попробовать архивировать проект с незавершённой заявкой → получить блокировку; перевести все заявки в `CLOSED` / `CANCELLED` → архивирование становится доступно.
6. Смета: создать ещё одну версию, назначить её основной, удалить неосновную, затем подтянуть позиции из выбранной заявки проекта и скачать PDF; проверить, что по умолчанию открывается/экспортируется основная версия.
7. Тайминг: день + слоты → убедиться, что новый слот предзаполняется от окончания предыдущего, список читается как `интервал | описание`, затем скачать `.docx` и открыть в Word.
8. Очередь заявок: открыть вкладки **«Активные»** и **«Архив»**, убедиться, что проектные заявки подсвечены фиолетовым и показывают название проекта.

### Регрессия (не сломать старое)

1. Создать **внешнюю заявку без проекта** — как раньше (заказчик, резерв, статусы).
2. **Greenwich**: не видит проекты; в API заявки для Greenwich нет лишнего `project` (кроме согласованного контракта для склада).
3. Напоминания / аудит инвентаря / cron — без ошибок из-за новых таблиц.

### Перед merge в `master` (из foundation)

Сверка с [`projects-foundation.md`](./projects-foundation.md) §8: создание заявок без проекта, Greenwich, резервы/P2034, ключи Storage, `brain:inventory`.

---

## 4. Зависимости npm

После `git pull` выполните **`npm install`** — для сметы и тайминга: **`pdf-lib`**, **`@pdf-lib/fontkit`** (встраивание шрифта в PDF), **`docx`**. В репозитории лежат **`src/server/fonts/NotoSans-*.ttf`** (SIL OFL) — без них PDF с кириллицей не соберётся.
